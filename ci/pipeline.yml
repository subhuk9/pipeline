---
resource_types:
  - name: npm-cache
    type: docker-image
    source:
      repository: ymedlop/npm-cache-resource
      tag: latest

resources:
  - name: concourse_pipelien1
    type: git
    source: &repo-source
      uri: https://github.com/subhuk9/pipeline
      branch: pipiline
  - name: dependency-cache
    type: npm-cache
    source:
      <<: *repo-source
      paths:
        - package.json

jobs:
  - name: Install dependencies
    plan:
      - get: concourse_pipelien1
        trigger: true
      - get: dependency-cache
  - name: Run tests
    plan:
      - get: concourse_pipelien1
        trigger: true
        passed: [Install dependencies]
      - get: dependency-cache
        passed: [Install dependencies]
      - task: run the test suite
        file: concourse_pipelien1/ci/tasks/run_tests.yml